import WrapperOptions, { MongooseModelRead, WrapperOptionsInput } from './wrapperOptions';
import { ObjectsKeys } from './helpers';
export interface WrapperDeleteOptionsInput<T> extends WrapperOptionsInput<T> {
    options?: {
        populateFields?: ObjectsKeys<T>[];
    };
    mongooseModel?: MongooseModelRead;
}
export default class WrapperDeleteOptions<T> extends WrapperOptions<T> {
    options: {
        populateFields?: ObjectsKeys<T>[];
    };
    mongooseModel: MongooseModelRead;
    constructor(input: WrapperDeleteOptionsInput<T>);
}
