import { SaveOptions } from 'mongoose';
import { QueryOptions } from 'mongoose';
export interface MongooseModelAdditions {
    MongoBouncer?: any;
}
export declare type MongooseModel = (QueryOptions | SaveOptions) & MongooseModelAdditions;
export declare type MongooseModelCreate = SaveOptions & MongooseModelAdditions;
export declare type MongooseModelRead = QueryOptions & MongooseModelAdditions;
export interface WrapperOptionsInput<T> {
    useMongoBouncer?: boolean;
    mongooseModel?: MongooseModel;
}
export default class WrapperOptions<T> {
    private _internals;
    mongooseModel: MongooseModel;
    constructor(input: WrapperOptionsInput<T>);
    get useMongoBouncer(): boolean;
    set useMongoBouncer(val: boolean);
}
