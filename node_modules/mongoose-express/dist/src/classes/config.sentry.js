"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
class ConfigSentry {
    constructor(env = process.env) {
        this._env = env;
    }
    get tracesSampleRate() {
        return this._env.NODE_ENV === 'production'
            ? this._env.SENTRY_TRACES_SAMPLE_RATE
                ? parseFloat(this._env.SENTRY_TRACES_SAMPLE_RATE)
                : 0.2
            : 1;
    }
    get dsn() {
        return this._env.SENTRY_DSN || '';
    }
    updateEnvironment(env = process.env) {
        this._env = env;
    }
}
exports.default = ConfigSentry;
//# sourceMappingURL=config.sentry.js.map