import { WrapperFindOptionsInput } from '../../classes/wrapperFindOptions';
import { Document, Model } from 'mongoose';
import { QuerySelector } from '../../classes/queries';
import { TransactionLike } from '../../classes/sentry';
declare const _default: <T, U = T>(model: Model<Document, Record<string, unknown>>, options?: WrapperFindOptionsInput<T, U, U, U>, existingTransaction?: TransactionLike | undefined) => (query: QuerySelector<T>) => Promise<U[]>;
export default _default;
