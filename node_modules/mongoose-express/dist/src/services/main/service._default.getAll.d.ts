import { TransactionLike } from '../../classes/sentry';
import { WrapperFindOptionsInput } from '../../classes/wrapperFindOptions';
import { Document, Model } from 'mongoose';
declare const _default: <T, U = T>(model: Model<Document, Record<string, unknown>>, options?: WrapperFindOptionsInput<T, U, U, U>, existingTransaction?: TransactionLike | undefined) => () => Promise<U[]>;
export default _default;
