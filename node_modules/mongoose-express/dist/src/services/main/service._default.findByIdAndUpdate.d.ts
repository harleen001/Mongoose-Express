import { Document, Model } from 'mongoose';
import { WrapperUpdateOptionsInput } from '../../classes/wrapperFindOneByIdAndUpdateOptions';
import { UpdateQuery } from '../../classes/queries';
import { TransactionLike } from '../../classes/sentry';
declare const _default: <T, U = T>(model: Model<Document, Record<string, unknown>>, options?: WrapperUpdateOptionsInput<T, U>, existingTransaction?: TransactionLike | undefined) => (id: string, item: UpdateQuery<T>) => Promise<U | null>;
export default _default;
