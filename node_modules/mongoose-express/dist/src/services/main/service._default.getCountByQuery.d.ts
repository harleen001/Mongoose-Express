import { TransactionLike } from '../../classes/sentry';
import { WrapperFindOptionsInput } from '../../classes/wrapperFindOptions';
import { Document, Model } from 'mongoose';
declare const _default: <T>(model: Model<Document, Record<string, unknown>>, options: WrapperFindOptionsInput<T, T, T, T>, existingTransaction?: TransactionLike | undefined) => (query: Record<string, any>) => Promise<number>;
export default _default;
