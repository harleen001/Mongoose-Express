import M = require('mongoose');
import { Mongoose as _Mongoose } from 'mongoose';
declare const Mongoose: typeof M;
export declare function connect(connectionString: string, mongoose: _Mongoose): Promise<void>;
export default Mongoose;
export declare const connectToDb: (connectionString?: string, mongoose?: typeof M) => void;
