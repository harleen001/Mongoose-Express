import { Schema } from 'mongoose';
import QbConfig from '../../../classes/class.QbConfig';
declare const preSaveX: (schema: Schema, config: QbConfig, operation: 'save') => Promise<void>;
export default preSaveX;
