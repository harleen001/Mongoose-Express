import { Schema } from 'mongoose';
import QbConfig from '../../classes/class.QbConfig';
declare const preUpdateMany: (schema: Schema, config: QbConfig) => Promise<void>;
export default preUpdateMany;
